.mt-5.w-500p
  .bg-white.border.rounded-3.mt-3.p-3
    %p.fs-2.text-center アカウント作成
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: false }) do |f|
      = f.input :username, label_html: { class: 'fs-5 lh-base' }
      = f.input :email, label_html: { class: 'fs-5 lh-base mt-3' }
      = f.input :password, required: true, label_html: { class: 'fs-5 lh-base mt-3' }
      - if @minimum_password_length
        %small.c-gray
          = @minimum_password_length
          文字以上で設定してください
      = f.input :password_confirmation, required: true, label_html: { class: 'fs-5 lh-base mt-3' }
      .text-center
        = f.button :submit, 'アカウント作成', class: 'btn btn-primary c-white mt-4'
      .text-center.mt-3
        = render 'users/shared/links'
